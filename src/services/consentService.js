// Consent service placeholder
// Use this to persist and retrieve consent records.

export const saveConsentRecord = async (registrationId, videoUrl) => {
  // TODO: save consent metadata
  return { success: true, registrationId, videoUrl };
};

export const listConsentRecords = async () => {
  // TODO: fetch existing consent records
  return [];
};

