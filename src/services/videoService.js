// Video service placeholder
// Implement actual media capture and upload when backend is available.

export const startRecording = async () => {
  // TODO: integrate with MediaRecorder or native SDK
  return { success: true };
};

export const stopRecording = async () => {
  // TODO: stop recording and return stored reference
  return { success: true, url: '' };
};

